% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rename.R
\name{rename}
\alias{rename}
\title{Rename data frame columns.}
\usage{
rename(data, data_rename, normalise_data_colnames = TRUE, drop = TRUE)
}
\arguments{
\item{data}{A data frame.}

\item{data_rename}{A correspondance table between old and new column names.}

\item{normalise_data_colnames}{If \code{TRUE} then all the data column names are normalised.}

\item{drop}{If \code{TRUE} then all the columns not present in data_rename are removed.}
}
\value{
A renamed data frame.
}
\description{
The data frame columns are renamed according to a correspondance table.\cr
The correspondance table data_remane must at least contains tow columns : \code{column} for the old column names and \code{rename} for the new column names.
}
\examples{
dplyr::tibble(var1 = "a", old = "b") \%>\%
  patchr::rename(dplyr::tibble(column = "old", rename = "new"), drop = FALSE)

}
